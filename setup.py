from setuptools import find_packages, setup

setup(
    name='netbox-resiliodb',
    version='0.1',
    description='ResilioDB Netbox Integration',
    install_requires=[],
    packages=find_packages(),
    include_package_data=True,
    zip_safe=False,
)
